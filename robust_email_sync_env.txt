# Robust Email Sync Environment Configuration
# Add these settings to your .env file on Linux server

# === ROBUST CONNECTION SETTINGS ===
MAIL_BATCH_SIZE=10
MAIL_MAX_SYNC_LIMIT=10
MAIL_MAX_SYNC_DAYS=5
MAIL_PROCESS_TIMEOUT=600
MAIL_MEMORY_LIMIT=1G

# === CONNECTION MANAGEMENT ===
MAIL_CONNECTION_STRATEGY=fresh_per_batch
MAIL_CONNECTION_TIMEOUT=30
MAIL_CONNECTION_RETRIES=3
MAIL_CONNECTION_RETRY_DELAY=2

# === PYTHON ENVIRONMENT ===
# Replace with your actual server path
MAIL_PYTHON_EXECUTABLE=/home/migratio/public_html/bansal_immigration/python_outlook_web/venv/bin/python
MAIL_AUTO_DETECT_PYTHON=true

# === DEBUG AND LOGGING ===
MAIL_ENABLE_DEBUG_LOGGING=true

# === PERFORMANCE OPTIMIZATIONS ===
MAIL_ENABLE_S3_UPLOAD=true
MAIL_ENABLE_LOCAL_STORAGE=true
MAIL_ENABLE_CONTENT_STORAGE=true
MAIL_PROCESS_ATTACHMENTS=true
MAIL_MAX_ATTACHMENTS_PER_EMAIL=5
MAIL_MAX_ATTACHMENT_SIZE=5242880

